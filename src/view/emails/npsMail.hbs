<style>
	div.container {
		align-items: center;
		display: flex;
		flex-direction: column;
		justify-content: center;
		margin: 0 auto;
		width: 800px;
	}

	div.container div.answers {
		display: flex;
		justify-content: space-between;
		width: 377px;
	}

	div.container div.answers a {
		background-color: #8257e6;
		border-radius: 8px;
		color: #fff;
		padding: 10px;
		text-align: center;
		text-decoration: none;
		width: 15px;
	}

	div.container div.answers a:hover {
		filter: brightness(0.8);
	}

	div.container div.level {
		display: flex;
		justify-content: space-between;
		margin: 10px;
		width: 350px
	}
</style>

<div class="container">
	<label>Olá <strong>{{name}}</strong>! Tudo bem?</label>

	<h3>{{title}}</h3>

	<br />

	<strong>{{description}}</strong>

	<div class="level">
		<span>Pouco provável</span>
		<span>Muito prováve</span>
	</div>

	<div class="answers">
		<a href="{{link}}/1?u={{user_id}}">1</a>
		<a href="{{link}}/2?u={{user_id}}">2</a>
		<a href="{{link}}/3?u={{user_id}}">3</a>
		<a href="{{link}}/4?u={{user_id}}">4</a>
		<a href="{{link}}/5?u={{user_id}}">5</a>
		<a href="{{link}}/6?u={{user_id}}">6</a>
		<a href="{{link}}/7?u={{user_id}}">7</a>
		<a href="{{link}}/8?u={{user_id}}">8</a>
		<a href="{{link}}/9?u={{user_id}}">9</a>
		<a href="{{link}}/10?u={{user_id}}">10</a>
	</div>

	<br />
	<br />

	<strong>Sua opnião é muito importante para nós</strong>
	<h3>Equipe <strong>NLW</strong></h3>
</div>